<div class="text-center">

<h1>Here Are Your Potential Fosters:</h1>

<% if @search.search_users.empty? %>
  <p>I'm sorry, but we couldn't find anyone that matches your criteria.</p>
<% else %>

    <% @search.search_users.each do |user| %>
      <div class="user-result large-4 large-offset-4">
        <h3><%= user.name  %></h3>
        <p><%= user.city %>, <%= user.state %></p>
        <p><%= link_to user.email, user_path(user)  %></p>
      </div>
    <% end %>

    <p><strong>
      <%= link_to "NOTIFY", :controller => "searches", :action => "send_mail", :id => @search.id %>
    </strong></p>

  <p>
    * By clicking 'Notify', you are sending out an email to these users to ask
    them if they would be interested in fostering for your organization.  Please
    do so respectfully.
  </p>

<% end %>

<p><%= link_to "Back", new_search_path %></p>
</div>
