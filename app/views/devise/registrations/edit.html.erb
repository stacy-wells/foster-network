<div class="large-10 large-offset-1 edit">
  <div class="text-center">
    <h2>Edit your information here:</h2>
  </div>

  <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
    <%= devise_error_messages! %>

    <div class="field large-8 large-offset-2 medium-4 medium-offset-4">
      <%= f.label :email %>
      <%= f.email_field :email, autofocus: true %>
    </div>

    <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
      <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
    <% end %>

    <div class="field large-8 large-offset-2 medium-4 medium-offset-4">
      <%= f.label :first_name, "First Name" %>
      <%= f.text_field :first_name, autocomplete: "off" %>
    </div>

    <div class="field large-8 large-offset-2 medium-4 medium-offset-4">
      <%= f.label :last_name, "Last Name" %>
      <%= f.text_field :last_name, autocomplete: "off" %>
    </div>

    <div class="field large-8 large-offset-2 medium-4 medium-offset-4">
      <%= f.label :city %>
      <%= f.text_field :city, autocomplete: "off" %>
    </div>

    <div class="field large-8 large-offset-2 medium-4 medium-offset-4">
      <%= f.label :state %>
      <%= f.text_field :state, autocomplete: "off" %>
    </div>

    <div class="field large-8 large-offset-2 medium-4 medium-offset-4">
      <%= f.label :password, "Password (leave blank if you don't want to change it)" %>
      <%= f.password_field :password, autocomplete: "off" %>
    </div>

    <div class="field large-8 large-offset-2 medium-4 medium-offset-4">
      <%= f.label :password_confirmation, "Password Confirmation" %>
      <%= f.password_field :password_confirmation, autocomplete: "off" %>
    </div>

    <div class="field large-8 large-offset-2 medium-4 medium-offset-4">
      <%= f.label :current_password, "Current Password (we need your current password to confirm your changes)" %>
      <%= f.password_field :current_password, autocomplete: "off" %>
    </div>

    <div class="actions text-center">
      <%= f.submit "Update", class: "button" %>
    </div>
  <% end %>

  <div class="text-center">
    <br>
    <p>Unhappy with your account? <%= button_to "Cancel my account", registration_path(resource_name), class: "button tiny", data: { confirm: "Are you sure?" }, method: :delete %></p>

    <%= link_to "Back", :back %>
  </div>
</div>
